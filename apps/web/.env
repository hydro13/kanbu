# Kanbu Web Environment Variables

# API URL - leave empty for automatic detection based on hostname
# Only set this if you need to override (e.g., production behind a proxy)
# VITE_API_URL="http://localhost:3001"
