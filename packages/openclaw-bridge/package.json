{
  "name": "@kanbu/openclaw-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "OpenClaw gateway bridge for Kanbu — dispatch tasks to AI agents via HTTP",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.16"
  }
}
